rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - allow all read/write
    // Ganti dengan rules ketat sebelum production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
